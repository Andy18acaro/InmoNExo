# Roadmap

Work is tracked as tracer-bullet tickets under `.scratch/mvp/` via mattpocock skills (`to-spec` / `to-tickets` / `implement`).

Intended sequence:

1. Align docs + audit (this bootstrap)
2. Schema + migrations
3. Grupo MG Provider end-to-end was planned; **audit chose HL.DI first** → DB + MarketEvents
4. Remaining four Providers
5. FastAPI read/analytics endpoints
6. Next.js dashboard (overview + project explorer)

Out of scope until later: maps, auth, payments, LLM-default extraction, nationwide coverage, real-time scraping.
